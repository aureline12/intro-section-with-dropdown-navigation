const showMenu = () => {
    document.getElementById('navbarContent').classList.add('show-menu');
}

const hideMenu = () => {
    document.getElementById('navbarContent').classList.remove('show-menu');
}